import React from 'react'
import { render } from 'react-dom'

export default function CorporationBank(props) {
    return (
        <div>
            <h1>Welcome to corporation bank located at {props.location}</h1>
        </div>
    )
}
